<%- include ('../../partials/header.ejs') %>
<%- include ('../../partials/navbar.ejs') %>

<div class="container">
    
    <hr>
    <h2>Artigos</h2>
    <a class="btn btn-primary" style="border: #000 2px solid" href="/admin/articles/new">Criar nova artigo</a>
    <hr>


    <table class="table table-bordered">
        <thead>
            <tr>
                <th style="border: #000 4px solid">ID</th>
                <th style="border: #000 4px solid">Título</th>
                <th style="border: #000 4px solid">Slug</th>
                <th style="border: #000 4px solid">Categoria</th>
                <th style="border: #000 4px solid">Ações</th>
            </tr>
        </thead>
        <tbody>
     <% articles.forEach(article => { %>
            <tr>
                <th style="border: #000 1px solid"><%= article.id %></th>
                <th style="border: #000 1px solid"><%= article.title %></th>
                <th style="border: #000 1px solid"><%= article.slug %></th>
                <th style="border: #000 1px solid"><%= article.category.title %></th>
                <th style="border: #000 1px solid">
                    <a href="/admin/articles/edit/<%= article.id %>" class="btn btn-warning" style="border: #000 2px solid">Editar</a>
                    
                    <form method="POST" action="/articles/delete" style="display: inline" onsubmit="confirmarDelecao(event, this)">
                       <input type="hidden" name="id" value="<%= article.id %>">
                        <button class="btn btn-danger" style="border: #000 2px solid">Deletar</button>
                    </form>

                </th>
            </tr>
    <% }) %>
        </tbody>
    </table>

</div>

<script>
    function confirmarDelecao(event, form){
        event.preventDefault();
        var decision = confirm("Você quer deletar esse artigo?");

            if(decision){
                form.submit();
            }else{

            }
    }
</script>

<%- include ('../../partials/footer.ejs') %>

